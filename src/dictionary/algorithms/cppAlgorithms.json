[

    "int binarySearch(int x[x], int n, int k) {\n    int l = 0, r = n - 1;\n    while (l <= r) {\n        int m = (l + r) / 2;\n        if (x[m] == k) return x[m];\n        if (x[m] < k)\n            l = m + 1;\n        else\n            r = m - 1;\n    }\n    return -1;\n}"

]