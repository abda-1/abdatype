import React from 'react';

import '../stylesheets/Footer.scss';

const Footer = () => {
    return (
        <footer>
            created by abdullah tahir
        </footer>
    );
};

export default Footer;